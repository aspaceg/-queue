#include <stdlib.h>

#ifndef
#define
typedef void* data

typedef struct op {
  int capacity;
  int top;
  int  * priority;
  char * symbol;
}
typedef struct numb {
  int capacity;
  int top;
  int * operand;
}

#endif
